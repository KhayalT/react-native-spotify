import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
    title: {
        color: 'whitesmoke',
        margin: 10,
        fontSize: 25,
        fontWeight: 'bold',
    },
    container: {
        margin: 10,
    }
});

export default styles;